# LiveKit Server 配置（方案A - 自建）

port: 7880
rtc:
  port_range_start: 50000
  port_range_end: 60000
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true

# LiveKit 密钥配置（方案A自建）
keys:
  devkey: TongYuanLiveKit@2026SecretKey

room:
  auto_create: true
  empty_timeout: 300  # 5分钟无参与者自动关闭房间

logging:
  level: info
  pion_level: warn
